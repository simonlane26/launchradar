import { IconSparkles } from "@tabler/icons-react";
import { ComingSoon } from "../coming-soon";

export default function CreatePage() {
  return (
    <ComingSoon
      icon={IconSparkles}
      feature="Create"
      phase={4}
      blurb="One place for everything the agent drafts for you — comparison pages, SEO landing pages, launch posts, founder story, changelog and build-in-public content — generated from your product and your backlog, ready to edit and ship."
    />
  );
}
